// Temporary feature flags

/// When false, the paywall UI and entitlement checks are bypassed.
/// Set to true for production releases with in-app purchases
const bool kPaywallEnabled = false;


/// Show debug overlays for decoration collision footprints
const bool kShowDecorationFootprints = true;

/// Show red collision boxes for decoration objects specifically
const bool kShowDecorationObjectFootprints = false;


